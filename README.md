# Bioacoustic project using AudioCLIP

## Usage

### Install with Docker

Clone the repository:

``````
git clone https://gitlab.com/nina-data/AudioCLIP/-/tree/list_pipeline
```

Build the image

```
cd AudioCLIP
docker build -t audioclip -f Dockerfile .
```
